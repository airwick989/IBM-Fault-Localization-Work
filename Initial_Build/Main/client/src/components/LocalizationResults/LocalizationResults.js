import { useEffect, useState } from "react";
import { useHistory } from "react-router-dom";
import axios from 'axios';

const LocalizationResults = () => {
    return ( 
        <div>
            <h1>Completed until Localization</h1>
        </div>
    );
}

export default LocalizationResults;