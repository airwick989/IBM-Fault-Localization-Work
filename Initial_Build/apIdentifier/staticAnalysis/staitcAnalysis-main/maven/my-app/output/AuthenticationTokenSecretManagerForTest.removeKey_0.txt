looking:AuthenticationTokenSecretManagerForTest.removeKey
@Override
synchronized boolean removeKey(Integer keyId) {
    boolean b = super.removeKey(keyId);
    if (b) {
        latch.countDown();
    }
    return b;
}

@Override
synchronized boolean removeKey(Integer keyId) {
    boolean b = super.removeKey(keyId);
    if (b) {
        latch.countDown();
    }
    return b;
}
possible Hot1
