looking:SortedList.get
@Override
public synchronized E get(int index) {
    {
        // FindBugs: UG_SYNC_SET_UNSYNC_GET complaint. Fix!!
        return list;
    }
}

@Override
public synchronized E get(int index) {
    return list.get(index);
}
possible Hot1
