looking:sf
synchronized (sf) {
    if (count < refSFCount) {
        assertTrue(sf.isReferencedInReads());
    } else {
        assertFalse(sf.isReferencedInReads());
    }
}

synchronized (sf) {
    if (count < refSFCount) {
        assertTrue(sf.isReferencedInReads());
    } else {
        assertFalse(sf.isReferencedInReads());
    }
}
possible Hot2
