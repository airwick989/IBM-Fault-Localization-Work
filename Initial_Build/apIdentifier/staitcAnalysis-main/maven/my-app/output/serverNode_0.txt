looking:serverNode
RegionStates	setServerState
-----------------------
synchronized (serverNode) {
    serverNode.setState(state);
}
-----------------------
synchronized (serverNode) {
    serverNode.setState(state);
}-----------------------
possible Hot2
