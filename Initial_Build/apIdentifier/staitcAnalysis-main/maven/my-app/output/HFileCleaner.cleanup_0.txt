looking:HFileCleaner.cleanup
@Override
public synchronized void cleanup() {
    {
        running = false;
        LOG.debug("Stopping file delete threads");
        for (Thread thread : threads) {
            thread.interrupt();
        }
    }
    stopHFileDeleteThreads();
}

@Override
public synchronized void cleanup() {
    super.cleanup();
    stopHFileDeleteThreads();
}
possible Hot1
possible type Hot3_2
1
