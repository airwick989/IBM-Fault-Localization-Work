looking:AbstractWALRoller.requestRollAll
synchronized (this) {
    for (RollController controller : wals.values()) {
        {
            this.rollRequest.set(true);
        }
    }
    notifyAll();
}

synchronized (this) {
    for (RollController controller : wals.values()) {
        controller.requestRoll();
    }
    notifyAll();
}
possible Hot1
possible type Hot3_2
1
