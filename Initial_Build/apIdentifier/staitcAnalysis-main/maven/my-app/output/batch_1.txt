looking:batch
synchronized (batch) {
    while (batch.installed != batch.error) {
        batch.wait();
    }
}

synchronized (batch) {
    while (batch.installed != batch.error) {
        batch.wait();
    }
}
possible Hot2
possible type Hot3_2
1
