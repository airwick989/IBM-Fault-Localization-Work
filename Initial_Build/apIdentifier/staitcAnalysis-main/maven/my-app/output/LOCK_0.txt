looking:LOCK
NamedQueueRecorder	getInstance
-----------------------
synchronized (LOCK) {
    if (!isInit) {
        namedQueueRecorder = new NamedQueueRecorder(conf);
        isInit = true;
    }
}
-----------------------
synchronized (LOCK) {
    if (!isInit) {
        namedQueueRecorder = new NamedQueueRecorder(conf);
        isInit = true;
    }
}-----------------------
possible Hot1
