looking:ReadOnlyConfiguration.getPropertySources
@Override
public synchronized String[] getPropertySources(String name) {
    {
        return conf.getPropertySources(name);
    }
}

@Override
public synchronized String[] getPropertySources(String name) {
    return conf.getPropertySources(name);
}
possible Hot1
