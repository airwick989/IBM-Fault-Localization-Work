looking:usedWorkers
synchronized (usedWorkers) {
    {
        if (!event.isReady()) {
            event.wake(scheduler);
        }
    }
    usedWorkers.wake(scheduler);
}

synchronized (usedWorkers) {
    usedWorkers.release(worker);
    usedWorkers.wake(scheduler);
}
possible Hot2
